
const express = require("express");

const dataController = async (req,res) => {
    try {
      
        res.send()
        
    } catch (e) {
        console.log(e)
    }
}

module.exports = {dataController}